---

---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<meta name="generator" content={Astro.generator} />
		<title>interactions.work — Strengthen how your team works together</title>
		<meta name="description" content="A mobile app for any group of humans to build shared values, appreciate each other, give honest feedback, and grow together. Free forever." />
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet" />
	</head>
	<body>
		<!-- Hero -->
		<section class="hero">
			<div class="container">
				<h1>Your team's interactions<br /><span class="highlight">matter more than metrics.</span></h1>
				<p class="subtitle">A mobile app for any group of humans to build shared values, appreciate each other, give honest feedback, and grow together.</p>
				<div class="cta-group">
					<a href="#download" class="cta-button primary">Start Free Now</a>
					<span class="cta-note">Free forever. No credit card.</span>
				</div>
				<div class="store-badges">
					<a href="#" class="store-badge" aria-label="Download on the App Store">
						<span class="badge-icon">&#xF8FF;</span> App Store
					</a>
					<a href="#" class="store-badge" aria-label="Get it on Google Play">
						<span class="badge-icon">&#9654;</span> Google Play
					</a>
				</div>
			</div>
		</section>

		<!-- Features -->
		<section class="features" id="features">
			<div class="container">
				<h2>What makes interactions.work different?</h2>
				<div class="feature-grid">
					<div class="feature-card">
						<div class="feature-emoji">📜</div>
						<h3>Shared Values</h3>
						<p>Create a team manifesto — the values you all commit to. Not a document on a wall. A living part of your daily interactions.</p>
					</div>
					<div class="feature-card">
						<div class="feature-emoji">👏</div>
						<h3>Appreciate & Grow</h3>
						<p>Send kudos tied to your team's values. <em>"Thanks for showing Trust."</em> Give honest feedback when it matters. Even the hard conversations.</p>
					</div>
					<div class="feature-card">
						<div class="feature-emoji">🎯</div>
						<h3>Goals Together</h3>
						<p>Set team objectives and personal growth goals — both linked to what your team values most. Track progress through self-reflection, not dashboards.</p>
					</div>
				</div>
			</div>
		</section>

		<!-- Use Cases -->
		<section class="use-cases">
			<div class="container">
				<h2>For any team that cares</h2>
				<p class="section-subtitle">Not just companies. Any group that wants to be intentional about how they work together.</p>
				<div class="use-case-grid">
					<div class="use-case">
						<span class="use-case-emoji">💻</span>
						<h4>Dev Teams & Agencies</h4>
						<p>Culture without the enterprise bloat</p>
					</div>
					<div class="use-case">
						<span class="use-case-emoji">⚽</span>
						<h4>Sports Teams & Clubs</h4>
						<p>Strengthen the bond beyond the field</p>
					</div>
					<div class="use-case">
						<span class="use-case-emoji">🤝</span>
						<h4>Volunteer Groups</h4>
						<p>Appreciate the people who show up</p>
					</div>
					<div class="use-case">
						<span class="use-case-emoji">🌍</span>
						<h4>Cross-Org Collaborations</h4>
						<p>Build culture without shared infrastructure</p>
					</div>
				</div>
			</div>
		</section>

		<!-- Privacy -->
		<section class="privacy">
			<div class="container">
				<div class="privacy-content">
					<h2>Your data stays yours.</h2>
					<p>interactions.work is device-first. Your data lives on your phone, not our servers. When it syncs, it's end-to-end encrypted — we literally cannot read it.</p>
					<ul class="privacy-list">
						<li><strong>Device-first storage</strong> — data lives on your phone</li>
						<li><strong>End-to-end encrypted</strong> — we can't read your interactions</li>
						<li><strong>EU hosted</strong> — all infrastructure in Germany</li>
						<li><strong>GDPR compliant</strong> — minimal data, full control</li>
					</ul>
				</div>
			</div>
		</section>

		<!-- Pricing -->
		<section class="pricing" id="pricing">
			<div class="container">
				<h2>Simple, honest pricing</h2>
				<p class="section-subtitle">No per-seat nonsense. No walls. No tricks.</p>
				<div class="pricing-grid">
					<div class="pricing-card">
						<h3>Free</h3>
						<div class="price">&euro;0</div>
						<div class="price-period">forever</div>
						<ul>
							<li>All core features</li>
							<li>Unlimited team members</li>
							<li>Encrypted P2P sync</li>
							<li>No limits, no walls</li>
						</ul>
						<a href="#download" class="cta-button secondary">Get Started</a>
					</div>
					<div class="pricing-card featured">
						<div class="badge">Most Popular</div>
						<h3>Pro</h3>
						<div class="price">&euro;10</div>
						<div class="price-period">per team / month</div>
						<ul>
							<li>Everything in Free</li>
							<li>Cloud sync & backup</li>
							<li>Analytics & insights</li>
							<li>Priority support</li>
						</ul>
						<a href="#download" class="cta-button primary">Start Free, Upgrade Later</a>
					</div>
					<div class="pricing-card">
						<h3>Community</h3>
						<div class="price">&euro;0</div>
						<div class="price-period">Pro features, free</div>
						<ul>
							<li>Everything in Pro</li>
							<li>For volunteer groups</li>
							<li>For community organizations</li>
							<li>Because you deserve it</li>
						</ul>
						<a href="#download" class="cta-button secondary">Get Started</a>
					</div>
				</div>
			</div>
		</section>

		<!-- Download CTA -->
		<section class="download" id="download">
			<div class="container">
				<h2>Start building a stronger team today.</h2>
				<p class="section-subtitle">Free forever. Two minutes to your first kudos.</p>
				<div class="store-badges large">
					<a href="#" class="store-badge" aria-label="Download on the App Store">
						<span class="badge-icon">&#xF8FF;</span> App Store
					</a>
					<a href="#" class="store-badge" aria-label="Get it on Google Play">
						<span class="badge-icon">&#9654;</span> Google Play
					</a>
				</div>
			</div>
		</section>

		<!-- Footer -->
		<footer>
			<div class="container">
				<div class="footer-content">
					<div class="footer-brand">
						<strong>interactions.work</strong>
						<p>Strengthen how your team works together.</p>
					</div>
					<div class="footer-links">
						<a href="/privacy">Privacy Policy</a>
						<a href="/terms">Terms of Service</a>
						<a href="/gdpr">GDPR</a>
						<a href="mailto:hello@interactions.work">Contact</a>
					</div>
				</div>
				<div class="footer-bottom">
					<p>&copy; 2026 interactions.work. Made with care in the Netherlands.</p>
				</div>
			</div>
		</footer>
	</body>
</html>

<style>
	:root {
		--bg: #0a0a0f;
		--bg-card: #12121a;
		--bg-card-hover: #1a1a28;
		--text: #e8e8ee;
		--text-muted: #8888a0;
		--accent: #e8a838;
		--accent-hover: #f0b848;
		--green: #34a853;
		--border: #222233;
		--radius: 12px;
	}

	* {
		margin: 0;
		padding: 0;
		box-sizing: border-box;
	}

	html {
		scroll-behavior: smooth;
	}

	body {
		font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
		background: var(--bg);
		color: var(--text);
		line-height: 1.6;
		-webkit-font-smoothing: antialiased;
	}

	.container {
		max-width: 1100px;
		margin: 0 auto;
		padding: 0 1.5rem;
	}

	/* Hero */
	.hero {
		padding: 8rem 0 5rem;
		text-align: center;
	}

	.hero h1 {
		font-size: clamp(2.2rem, 5vw, 3.5rem);
		font-weight: 800;
		line-height: 1.15;
		margin-bottom: 1.5rem;
		letter-spacing: -0.03em;
	}

	.highlight {
		color: var(--accent);
	}

	.subtitle {
		font-size: 1.2rem;
		color: var(--text-muted);
		max-width: 600px;
		margin: 0 auto 2.5rem;
		line-height: 1.7;
	}

	.cta-group {
		margin-bottom: 2rem;
	}

	.cta-button {
		display: inline-block;
		padding: 0.9rem 2.2rem;
		border-radius: 50px;
		font-weight: 600;
		font-size: 1rem;
		text-decoration: none;
		transition: all 0.2s ease;
		cursor: pointer;
	}

	.cta-button.primary {
		background: var(--accent);
		color: #0a0a0f;
	}

	.cta-button.primary:hover {
		background: var(--accent-hover);
		transform: translateY(-1px);
	}

	.cta-button.secondary {
		background: transparent;
		color: var(--text);
		border: 1px solid var(--border);
	}

	.cta-button.secondary:hover {
		border-color: var(--accent);
		color: var(--accent);
	}

	.cta-note {
		display: block;
		margin-top: 0.8rem;
		font-size: 0.85rem;
		color: var(--text-muted);
	}

	.store-badges {
		display: flex;
		gap: 1rem;
		justify-content: center;
		flex-wrap: wrap;
	}

	.store-badge {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		padding: 0.7rem 1.5rem;
		border: 1px solid var(--border);
		border-radius: 8px;
		color: var(--text);
		text-decoration: none;
		font-size: 0.9rem;
		transition: border-color 0.2s;
	}

	.store-badge:hover {
		border-color: var(--accent);
	}

	.store-badges.large .store-badge {
		padding: 1rem 2rem;
		font-size: 1.05rem;
	}

	/* Features */
	.features {
		padding: 5rem 0;
	}

	.features h2, .use-cases h2, .privacy h2, .pricing h2, .download h2 {
		font-size: clamp(1.8rem, 3vw, 2.5rem);
		font-weight: 700;
		text-align: center;
		margin-bottom: 1rem;
		letter-spacing: -0.02em;
	}

	.section-subtitle {
		text-align: center;
		color: var(--text-muted);
		font-size: 1.1rem;
		margin-bottom: 3rem;
	}

	.feature-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
		gap: 1.5rem;
		margin-top: 3rem;
	}

	.feature-card {
		background: var(--bg-card);
		border: 1px solid var(--border);
		border-radius: var(--radius);
		padding: 2rem;
		transition: border-color 0.2s, background 0.2s;
	}

	.feature-card:hover {
		border-color: var(--accent);
		background: var(--bg-card-hover);
	}

	.feature-emoji {
		font-size: 2.5rem;
		margin-bottom: 1rem;
	}

	.feature-card h3 {
		font-size: 1.2rem;
		font-weight: 600;
		margin-bottom: 0.8rem;
	}

	.feature-card p {
		color: var(--text-muted);
		font-size: 0.95rem;
		line-height: 1.6;
	}

	/* Use Cases */
	.use-cases {
		padding: 5rem 0;
	}

	.use-case-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
		gap: 1.5rem;
	}

	.use-case {
		text-align: center;
		padding: 2rem 1rem;
	}

	.use-case-emoji {
		font-size: 2.5rem;
		display: block;
		margin-bottom: 1rem;
	}

	.use-case h4 {
		font-size: 1.05rem;
		font-weight: 600;
		margin-bottom: 0.5rem;
	}

	.use-case p {
		color: var(--text-muted);
		font-size: 0.9rem;
	}

	/* Privacy */
	.privacy {
		padding: 5rem 0;
	}

	.privacy-content {
		max-width: 650px;
		margin: 0 auto;
	}

	.privacy-content h2 {
		text-align: left;
	}

	.privacy-content p {
		color: var(--text-muted);
		font-size: 1.05rem;
		margin-bottom: 2rem;
		line-height: 1.7;
	}

	.privacy-list {
		list-style: none;
	}

	.privacy-list li {
		padding: 0.8rem 0;
		border-bottom: 1px solid var(--border);
		font-size: 0.95rem;
		color: var(--text-muted);
	}

	.privacy-list li strong {
		color: var(--text);
	}

	/* Pricing */
	.pricing {
		padding: 5rem 0;
	}

	.pricing-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
		gap: 1.5rem;
		max-width: 900px;
		margin: 0 auto;
	}

	.pricing-card {
		background: var(--bg-card);
		border: 1px solid var(--border);
		border-radius: var(--radius);
		padding: 2.5rem 2rem;
		text-align: center;
		position: relative;
	}

	.pricing-card.featured {
		border-color: var(--accent);
	}

	.pricing-card .badge {
		position: absolute;
		top: -12px;
		left: 50%;
		transform: translateX(-50%);
		background: var(--accent);
		color: #0a0a0f;
		font-size: 0.75rem;
		font-weight: 600;
		padding: 0.3rem 1rem;
		border-radius: 50px;
	}

	.pricing-card h3 {
		font-size: 1.2rem;
		font-weight: 600;
		margin-bottom: 0.5rem;
	}

	.price {
		font-size: 2.8rem;
		font-weight: 800;
		color: var(--accent);
	}

	.price-period {
		color: var(--text-muted);
		font-size: 0.85rem;
		margin-bottom: 2rem;
	}

	.pricing-card ul {
		list-style: none;
		margin-bottom: 2rem;
	}

	.pricing-card ul li {
		padding: 0.5rem 0;
		font-size: 0.9rem;
		color: var(--text-muted);
	}

	/* Download */
	.download {
		padding: 5rem 0;
		text-align: center;
	}

	/* Footer */
	footer {
		padding: 3rem 0;
		border-top: 1px solid var(--border);
	}

	.footer-content {
		display: flex;
		justify-content: space-between;
		align-items: flex-start;
		flex-wrap: wrap;
		gap: 2rem;
		margin-bottom: 2rem;
	}

	.footer-brand p {
		color: var(--text-muted);
		font-size: 0.9rem;
		margin-top: 0.3rem;
	}

	.footer-links {
		display: flex;
		gap: 1.5rem;
		flex-wrap: wrap;
	}

	.footer-links a {
		color: var(--text-muted);
		text-decoration: none;
		font-size: 0.9rem;
		transition: color 0.2s;
	}

	.footer-links a:hover {
		color: var(--accent);
	}

	.footer-bottom {
		text-align: center;
		padding-top: 1.5rem;
		border-top: 1px solid var(--border);
	}

	.footer-bottom p {
		color: var(--text-muted);
		font-size: 0.8rem;
	}

	@media (max-width: 640px) {
		.hero {
			padding: 5rem 0 3rem;
		}

		.footer-content {
			flex-direction: column;
			align-items: center;
			text-align: center;
		}
	}
</style>
